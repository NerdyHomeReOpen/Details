# .env 檔案內容

# --- 模式選擇 ---
# 1. 首次設定請使用 'init'
# 2. 設定完成後, 改為 'production' 來啟動服務
MODE=production

NODE_VERSION=20

# --- Git 設定 ---
# 請務必替換成您的儲存庫 URL
GIT_REPO_URL=https://github.com/NerdyHomeReOpen/RiceCallAdminPanel
# 請替換成您要使用的分支
GIT_BRANCH=dev

# --- GitHub Actions Runner 設定 (可選) ---
# 在此處直接貼上您從 GitHub 取得的 Runner Token
# 如果不需要 Runner 功能, 請將此行留空
RUNNER_TOKEN=TOKEN

# --- 指令設定 ---
# 'init' 模式下, clone 專案後要執行的初始化指令
NODE_INIT_COMMAND=yarn install && yarn build
# 'production' 模式下, 要執行的應用程式啟動指令
STARTUP_COMMAND=yarn start